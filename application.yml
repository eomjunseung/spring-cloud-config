token:
  expiration_time: 864000000 
  secret: user_token_default

order_service:
  # url: http://127.0.0.1:8000/order-service/%s/orders
  url: http://ORDER-SERVICE/order-service/%s/orders
  exception:
    orders_is_empty : empty,,,,
gateway:
  ip: 128.0.0.1

